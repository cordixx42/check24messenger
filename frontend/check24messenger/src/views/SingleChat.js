import { useNavigate } from "react-router-dom";
import { useState } from "react";

export const SingleChat = () => {
  const navigate = useNavigate();

  return <h1>This should be overview of one single chat </h1>;
};
